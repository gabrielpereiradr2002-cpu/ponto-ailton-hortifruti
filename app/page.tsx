'use client';
import {useState} from 'react'; import {supabase} from '@/lib/supabase';
export default function Page(){const [registration,setRegistration]=useState(''),[password,setPassword]=useState(''),[msg,setMsg]=useState('');
async function login(){setMsg('Entrando...');const s=supabase();const email=`${registration.trim().toLowerCase()}@ponto.local`;const {data,error}=await s.auth.signInWithPassword({email,password});if(error)return setMsg('Matrícula ou senha inválida.');const {data:p}=await s.from('profiles').select('role,active').eq('id',data.user.id).single();if(!p?.active)return setMsg('Usuário inativo.');location.href=p.role==='admin'?'/admin':'/dashboard'}
return <main><div className="brand">Ailton Hortifruti</div><h1>Controle de Ponto</h1><p className="muted">Acesso de funcionários e administradores.</p><div className="card"><h2>Entrar</h2><label>Matrícula</label><input inputMode="numeric" placeholder="Ex.: 1001" value={registration} onChange={e=>setRegistration(e.target.value)}/><label>Senha</label><input type="password" placeholder="Sua senha" value={password} onChange={e=>setPassword(e.target.value)} onKeyDown={e=>e.key==='Enter'&&login()}/><button onClick={login}>Entrar</button>{msg&&<p>{msg}</p>}</div></main>}
